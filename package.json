{
  "name": "vitabahn-web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "VitaBahn HADP investor brief — static site plus Vercel serverless functions: lead form (api/lead.js) and the investor-access / data-room / founder-console system (api/*).",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --test \"test/**/*.test.js\"",
    "create-admin": "node scripts/create-admin.js"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0",
    "@vercel/analytics": "^2.0.1",
    "nodemailer": "^9.0.3"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.4"
  }
}
